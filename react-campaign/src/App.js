import React from 'react';

class App extends React.Component {
  render() {
    // console.log(web3.version);
    // web3.eth.getAccounts().then(console.log);

    return (
      <div>
        <h2>Campaign Contract (React)</h2>
      </div>
    );
  }
}
export default App;
